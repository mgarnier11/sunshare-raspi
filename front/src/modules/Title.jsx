import React from 'react';

const componentName = 'Title';
const size = { x: 8, y: 1 };

const Title = () => <h2>Efficacité des panneaux solaires</h2>;

const Module = {
  componentName,
  component: Title,
  size
};

export default Module;
